{
  "name": "renalroute-tests",
  "private": true,
  "description": "Test harness only. Kept in test/ so no package.json sits at the repo root, where Vercel would treat this static site as a Node build.",
  "scripts": {
    "test": "node verify.js && node e2e.js && node probe.js && node headers.js && node wiring.js && node sweep.js && node journey.js && node forms.js && node hostile.js && node a11y.js",
    "live": "node live.js",
    "verify": "npm test && npm run live"
  },
  "devDependencies": {
    "axe-core": "^4.12.1",
    "clean-css": "^5.3.3",
    "jsdom": "^30.0.0",
    "terser": "^5.49.2"
  },
  "dependencies": {
    "axe-core": "^4.12.1"
  }
}
